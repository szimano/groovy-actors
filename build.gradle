apply plugin: 'groovy'

version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    compile localGroovy()
    compile "org.codehaus.gpars:gpars:1.2.0"
    testCompile group: 'junit', name: 'junit', version: '4.11'
}